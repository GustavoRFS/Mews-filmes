{
    "version":2,
    "builds":[
        {"use":"npm run build"},
        {
            "src":"./src/index.js",
            "use":"@vercel/node"
        }
    ],
    "routes":[
        {
            "src":"./api/(.*)",
            "dest":"/"
        }
    ]
}